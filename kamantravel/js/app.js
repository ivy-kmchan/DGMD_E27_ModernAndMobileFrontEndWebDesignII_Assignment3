$(document).foundation();

/*$(document).foundation({
  tabs: {
    callback : function (tabs) {
      $(window).trigger('resize');
    }
  }
});

$(document).foundation({
  form: {
    callback : function (form) {
      $(window).trigger('resize');
    }
  }
});

$(document).foundation({
  tabs: {
    callback : function (tabs) {
      $(window).trigger('resize');
    }
  }
});*/